import { txLineClient } from './txline-client'; txLineClient.getHistoricalOdds(18185036).then(() => { const updates = (txLineClient as any).historicalOddsCache; const participantUpdates = updates.filter((u: any) => u.SuperOddsType === 'OVERUNDER_PARTICIPANT_GOALS'); console.log(JSON.stringify(participantUpdates.slice(0, 10).map((u:any) => ({params: u.MarketParameters, p1: u.ParticipantId, home: u.IsHome, all: Object.keys(u)})), null, 2)); })
